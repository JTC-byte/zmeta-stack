# Copy this file to .env and adjust values to suit your environment.
# Any variable you omit falls back to the defaults documented in README.md.

# --- Core service settings ---
ZMETA_APP_TITLE=ZMeta Backend
ZMETA_ENV=dev
ZMETA_UI_BASE_URL=http://127.0.0.1:8000

# --- UDP ingest ---
ZMETA_UDP_HOST=0.0.0.0
ZMETA_UDP_PORT=5005
ZMETA_UDP_QUEUE_MAX=4096

# --- WebSockets ---
ZMETA_WS_GREETING=Connected to ZMeta WebSocket
ZMETA_WS_QUEUE=64

# --- CORS / origins ---
# Comma-separated list, e.g. http://localhost:3000,http://127.0.0.1:3000
ZMETA_CORS_ORIGINS=*

# --- Auth (optional) ---
# Provide a shared secret to require clients to authenticate.
# ZMETA_SHARED_SECRET=super-secret-value
# ZMETA_AUTH_HEADER=x-zmeta-secret

# --- Simulators & UDP broadcasters ---
# If set, overrides the default UDP target used by simulators.
# When unset, they will fall back to ZMETA_UDP_TARGET_HOST -> ZMETA_UDP_HOST.
# ZMETA_SIM_UDP_HOST=
ZMETA_UDP_TARGET_HOST=127.0.0.1

# --- Recorder ---
# Uncomment to prune NDJSON files older than the stated number of hours.
# ZMETA_RECORDER_RETENTION_HOURS=72
